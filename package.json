{
  "name": "manifest-app",
  "version": "0.1.0",
  "description": "Production is our dev environment.",
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "test": "bun test",
    "manifest": "bun manifest/cli/index.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
